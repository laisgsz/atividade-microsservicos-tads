# atividade-microsservicos-tads
O código foi desenvolvido seguindo os padrões de FastAPI e Docker Compose, porém houve uma limitação técnica no ambiente local (erro de inicialização do Docker Desktop/WSL2) que impediu a geração dos prints de execução local.
